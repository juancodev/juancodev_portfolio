rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{document} {
      // Anyone can read project listing
      allow read: if true;
      // Only authenticated users can write/modify
      allow write: if request.auth != null;
    }
  }
}
